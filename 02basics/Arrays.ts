const superhero: Array<string> = [];
const PowerLevel: number[] = [];

type bool = boolean;;
type User = {
    name: string,
    is_Active: bool
}
type cardNumber ={
    cardNumber: string
}

superhero.push("Zoro");
PowerLevel.push(11000000000);

const allusers: User[]=[];

allusers.push({name: "", is_Active: false})

const MLMODELS: number[][] = [
    [255, 255, 255],
    [12,12,12]
]

export {}